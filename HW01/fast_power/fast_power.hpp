#include <string>
#include <chrono>

unsigned long long fast_power(unsigned long long,unsigned long long);

std::string to_binary_string(unsigned long long);

unsigned long long power(unsigned long long,unsigned long long);